# Haxe/hxnodejs externs for the [shortid](https://www.npmjs.com/package/shortid) npm library

Tested using `shortid` version **2.2.8**

Example:
```haxe
import js.npm.ShortId;

ShortId.generate();
```
